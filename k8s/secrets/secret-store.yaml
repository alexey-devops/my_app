apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: k8s-secret-store
  namespace: my-app
spec:
  provider:
    kubernetes:
      auth:
        serviceAccount:
          name: eso-secret-reader
          namespace: my-app
